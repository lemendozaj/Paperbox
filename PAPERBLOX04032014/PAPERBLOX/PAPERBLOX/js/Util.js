// JavaScript Document
var degrees = 1; // 1 degree
var radians = degrees * (Math.PI / 180);  //0.0175radians

function DegToRad(d) {	// Converts degrees to radians    
    return d * 0.0174532925199432957;
}

var rand = Math.floor(Math.random()*6);